
// Write your code here
    // Declare the variables num1 and num2 and assign them values
let num1 = 31;
let num2 = 2;
// Declare the variable multiply and assign it the product of num1 and num2
let multiply = num1 * num2;
// Declare the variable random and assign it a random integer greater than 0
let random = Math.floor(Math.random() * 100) + 1;

// Declare the variables num3 and num4 and assign them values
let num3 = 24;
let num4 = 5;
// Declare the variable mod and assign it the remainder of num3 divided by num4
let mod = num3 % num4;
// Declare the variable max and assign it the highest number in a set
let max = Math.max(5, 10, 15, 20);
